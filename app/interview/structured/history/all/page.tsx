import StructuredHistory from "@/app/components/interview/structured/history";

const Structured_History = () => {
  return (
    <div>
      <StructuredHistory />
    </div>
  );
};

export default Structured_History;
